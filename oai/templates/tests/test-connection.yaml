apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oai.fullname" . }}-test-connection"
  labels:
{{ include "oai.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "oai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
